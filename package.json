{
  "name": "advanced-crypto-trading-bot",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["frontend"],
  "scripts": {
    "build": "cd frontend && npm run build",
    "dev": "cd frontend && npm run dev",
    "start": "cd frontend && npm start"
  },
  "description": "Advanced crypto trading bot with Next.js frontend and Python FastAPI backend",
  "engines": {
    "node": ">=20.0.0"
  }
}